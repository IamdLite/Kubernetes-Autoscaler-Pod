# Node
node_modules/
npm-debug.log*
.npm/

# Results logs (keep the template, ignore the actual run outputs)
results/*.log
results/*.txt
results/*.png
results/*.jpg
!results/.gitkeep

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local kube config (never commit)
kubeconfig
*.kubeconfig
.kube/

# Secrets — should not exist here, but just in case
*.pem
*.key
.env
.env.local
